# HeartlineHK-Web
React App for HeartlineHK

tell Node to allow older crypto functions.
set NODE_OPTIONS=--openssl-legacy-provider

Deploy to firebase:
cd to either admin/web
npm run build

../ back then
firebase deploy --only hosting:admin
